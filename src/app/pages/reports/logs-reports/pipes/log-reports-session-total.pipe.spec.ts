import { LogReportsSessionTotalPipe } from './log-reports-session-total.pipe';

describe('LogReportsSessionTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new LogReportsSessionTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
