import { LogNotasSessionTotalPipe } from './log-notas-session-total.pipe';

describe('LogNotasSessionTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new LogNotasSessionTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
