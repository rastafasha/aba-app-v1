import { LogNotasUnitPricePipe } from './log-notas-unit-price.pipe';

describe('LogNotasUnitPricePipe', () => {
  it('create an instance', () => {
    const pipe = new LogNotasUnitPricePipe();
    expect(pipe).toBeTruthy();
  });
});
