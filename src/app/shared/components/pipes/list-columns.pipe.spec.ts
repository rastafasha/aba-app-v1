import { ListColumnsPipe } from './list-columns.pipe';

describe('ListColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
