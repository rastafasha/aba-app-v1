import { CellRenderPipe } from './cell-render.pipe';

describe('CellRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new CellRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
