import { FilterDisponibilidadCptPipe } from './filter-disponibilidad-cpt.pipe';

describe('FilterDisponibilidadCptPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDisponibilidadCptPipe();
    expect(pipe).toBeTruthy();
  });
});
